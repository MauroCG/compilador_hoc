func atan2(){
         if(b1>0){
                 return atan(a2/b1)
         } else if (b1<0){
                 return atan(a2/b1)+PI
         } else if (a2>0){
                 return PI/2
         } else if (a2<0){
                 return -PI/2
         } else {
                 print "atan2 domain error"
                 return 0
         }
 }
 atan()
 